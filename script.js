function changeImage(element) {

  /* (concaténer) récupère les attributs de l'élément pour le remplacer
  par un nouveau chemin, pour une nouvelle image. */
  element.src = 'images/' + element.id + '_2.jpg';
}
